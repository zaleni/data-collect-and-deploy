# 数据采集代码说明

> 代码都位于/home/pc3/data_collect/data_anno下

> 主要的代码存放在collect_datampy文件中，而界面描述存放在main.qml中

阅读代码所需的前置知识包括但不限于以下内容：
* 熟练使用Python语言
* 了解Ros的基本概念
* 了解Qt的基本概念
* 简单的代码抽象方式

如果在阅读代码中出现了疑问，可以从上述几个方向结合实际代码询问各种AI模型。

## 代码内容介绍

```python
ImageFolderName : Final[str] = 'img'
ImagePosFront   : Final[str] = 'front'
ImagePosWrist   : Final[str] = 'wrist'
ImageTypeRGB    : Final[str] = 'color'
ImageTypeDepth  : Final[str] = 'depth'

# ImagePos = Literal[ImagePosFront, ImagePosWrist]
ImagePos = Literal['front', 'wrist']
# ImageType = Literal[ImageTypeRGB, ImageTypeDepth]
ImageType = Literal['color', 'depth']

XRange = [-11005, 6312]
YRange = [-63615, -51151]
ZRange = [345710, 357259]
```

上面是24行到37行间的内容，用于定义一些常量。
其中Image相关的定义了文件夹名称、位置和拍摄图形类别的名称，后面后用于区分不同的图像。后面的XYZ Range代表初始位置，即每次采集前机械臂应当处于的位置。

```python
class ImageSource: ···
class LiveCameraSource(ImageSource): ···
class DiskImageSource(ImageSource): ···

class ImageProvider(QQuickImageProvider): ···
```

上面是39行到84行的内容，用于定义该软件用于展示的图像从何而来，两个子类分别表示相机来源的图像和先前保存的数据中来源的图像。

**ImageProvider**类型是用于向QtQuick提供图像的一个接口。

```python
class SimpleSaver(MessageSaver): ···
```

**SimpleSaver**用于实际执行实时保存采集到的数据的类，其基类定义于被我魔改过的piper sdk文件中，如果该类的一个实例正处于saving状态，它会保存所有接受到的信息到对应的文件夹，并且没有缓冲区。

```python
class RealTimeDataProvider(QObject): ···
```

**RealTimeDataProvider**用于从piper_sdk中实时读取机械臂的状态，并被用于在主界面上展示。

```python
class ViewModel(QObject): ···
```

**ViewModel**是主窗口对应的类，用于管理各种状态。

我认为在了解[该软件的功能](./数据采集软件.md)后能够轻易理解代码内容，所以这里就不赘述了。
