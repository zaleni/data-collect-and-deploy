   
├── 00-sh #编译脚本
├── ARX_CAN #CAN配置文件 （全局适用）
├── ARX_VR_SDK #VR SDK
├── py   #python SDK
├── readme  #文档合集
├── ROS   #ROS SDK
└── ROS2  #ROS2 SDK

注意机械臂型号，不同机械臂设备SDK不通用（X5只能使用X5 SDK,R5只能使用R5 SDK）
我们的sdk有三个平台：python, ROS, ROS2.
对应不同的使用场景，阅读对应的说明手册。
首先阅读“配置can手册”，因为无论应用哪种应用，都需要通过can通信。
不要有中文路径。


配置CAN手册 
配置can

python-单臂X5-SDK 
控制单臂

ROS1-单臂X5-SDK 
控制单臂

ROS2-单臂X5-SDK
控制单臂

X5-ROS-aloha 
四臂（VR）遥操作

X5-ROS2-aloha
四臂（VR）遥操作
